import PageShell from "@/components/PageShell";

export const metadata = { title: "Starter Page" };

export default function StarterPage() {
  return (
    <PageShell
      title="Starter Page"
      description="Starter content will be migrated into JSX only if needed."
    />
  );
}
